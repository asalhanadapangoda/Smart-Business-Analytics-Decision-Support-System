
services:
  db:
    image: postgres:15-alpine
    container_name: sbadss-db
    environment:
      POSTGRES_DB: sbadss
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: asal2003
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: sbadss-redis
    ports:
      - "6379:6379"

  ai-service:
    build: ./ai-service
    container_name: sbadss-ai
    ports:
      - "8000:8000"
    depends_on:
      - db

  backend:
    build: ./backend
    container_name: sbadss-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/sbadss
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: asal2003
      SPRING_DATA_REDIS_HOST: redis
      AI_SERVICE_URL: http://ai-service:8000
    depends_on:
      - db
      - redis
      - ai-service

  frontend:
    build: ./frontend
    container_name: sbadss-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  postgres_data:
